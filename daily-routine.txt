git checkout development > Switch to a new branch "development"
git pull
git checkout yourbranch > Switch to your own issue tackling now
git rebase development

work on your branch

git add .
git commit -m "make new change"

git push origin yourbranch

on GitHub repo-
Make a pull request to development branch

Note: You do NOT push directly to main, development, and staging branches